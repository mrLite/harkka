<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
	"http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
	<title>Timetable</title>
	<style type="text/css">
		body { font-family: Georgia; font-size: 1em; margin: 0; }

		h1 { font-size: 1.8em; }

		h2 { font-size: 1.3em; display: inline; }

		tr {}
		td {
			width: 150px;
			height: 5em;
			padding: 5px;
			border: 1px dotted silver;
			border-top: none; border-left: none;
		}
		td.klo { width: 50px; background-color: #DDC; font-weight: bold; border: none; text-align: center; }
		.LU { background-color: #ffc7c7; }
		.LH { background-color: #ffe9e9; }
		.lecturer { font-size: 0.7em; }
		.insignia { font-size: 2em; text-align: center; margin-top: 60px;}

		a, a:visited { text-decoration: none; border-bottom: 2px dotted silver; color: orange;}
		a:hover { text-decoration: none; border-bottom: 2px dotted gray; }

		#wrapper { 
			width: 800px;
			margin: 2em auto;
			border: 1px solid silver; border-top: none;
			background-color: #EED;
		}
	</style>
</head>

<body>
	<div id="wrapper">	
	<table>
		<tr id="1">
			<td class="klo"></td>
			<td class="klo">MA</td>
			<td class="klo">TI</td>
			<td class="klo">KE</td>
			<td class="klo">TO</td>
			<td class="klo">PE</td>
		</tr>
		<% @table_rows.each do |row| %>
			<tr>
				<% row.each do |slot| %>
					<% if slot.class == String %>
						<td class="klo"><%= slot %></td>
					<% elsif slot.class == Hash and !slot.empty? %>
						<%= "<td class=\""+slot[:type]+"\">" %><%= slot[:name] %><br /><%= slot[:location] %><br /><span class="lecturer"><%= slot[:lecturer] %></span></td>
					<% else %>
						<td></td>
					<% end %>
				<% end %>
			</tr>
		<% end %>
	</table>
	</div>
	<p class="insignia"><span class="LU">Luento</span> <span class="LH">Laskuharjoitus</span></p>
</body>
</html>
